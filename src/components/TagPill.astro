---
interface Props {
  tag: string;
  active?: boolean;
}

const { tag, active = false } = Astro.props;
---
<a
  href={`/tags/${tag}`}
  class={`tag-pill${active ? ' tag-pill-accent' : ''}`}
>#{tag}</a>
